{
  "name": "vue-02-01",
  "version": "1.0.0",
  "description": "első kalandozásunk npm földön",
  "main": "scripts-vue.js",
  "scripts": {
    "serve": "http-server -a localhost -p 8080 -s",
    "open": "opener http://localhost:8080",
    "sass": "node-sass --output-style compressed src/main.scss -o dist -w",
    "dev": "concurrently \"npm run sass\" \"npm run serve\" \"npm run open\""
  },
  "author": "kovacsaronlaszlo",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^4.1.3",
    "vue": "^2.5.17"
  },
  "devDependencies": {
    "concurrently": "^3.6.1",
    "http-server": "^0.11.1",
    "node-sass": "^4.9.3",
    "opener": "^1.5.0"
  }
}
